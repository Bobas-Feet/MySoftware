pipeline {
    agent any

    stages {
        stage('Call on you') {
            steps {
                sh 'python Click.py'
                sh 'python Welcome.py'                                
            }
        }
    }
}
