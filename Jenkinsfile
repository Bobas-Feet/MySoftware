pipeline {
    agent any

    stages {
        stage('Call on you') {
            steps {
                sh 'python.py'
                sh 'python.Click.py'
                sh 'python.Welcome.py'                                
            }
        }
    }
}
