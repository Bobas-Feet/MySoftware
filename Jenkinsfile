pipeline {
    agent any

    stages {
        stage('Call on you') {
            steps {
                sh 'python.py'
                sh 'python.Click'
                sh 'python.Welcome'                                
            }
        }
    }
}
