pipeline {
    agent any

    stages {
        stage('Call on you') {
            steps {
                sh 'python.py'
                Click()
                Welcome()                                
            }
        }
    }
}
